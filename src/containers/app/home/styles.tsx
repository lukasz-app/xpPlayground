import RX = require('reactxp');
export default {
  container: 
  RX.Styles.createViewStyle({
    padding: 16,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: 'yellow',
    flex:1
  }),
}
